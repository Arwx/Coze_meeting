# 会议主持人 Demo 方案 A Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将当前单文件前端 Demo 接入 Coze，实现会议转写清洗、结构化纪要生成、纪要写入 Coze 知识库、历史纪要查询，并完成可上线演示版本。

**Architecture:** 前端继续使用 `meeting-host-demo.html`，通过你自己的轻量 API 代理调用 Coze 工作流。Coze 承担业务后端能力：LLM 清洗、纪要生成、知识库写入、知识库检索；API 代理只负责隐藏 Coze Token、转发请求、规范化返回值。

**Tech Stack:** HTML/CSS/JavaScript 单页前端、Coze 智能体、Coze 工作流、Coze 知识库、Node.js Express 或 Vercel/Cloudflare Serverless API 代理。

## Global Constraints

- 全项目只开放一个业务角色：会议主持人。
- 前端不得直接保存或暴露 Coze Personal Access Token。
- 所有 Coze 工作流必须发布后再被 API 调用。
- 会议纪要必须使用 Markdown 输出。
- 历史查询必须基于 Coze 知识库召回内容回答，未命中时返回：`历史纪要中未检索到相关内容`。
- 第一版上线 Demo 允许继续使用模拟转写数据；真实 ASR 放到后续阶段。
- 如果当前 Coze 空间没有“知识库写入/新增文档”节点，方案 A 无法纯工作流闭环，需要改为后端调用 Coze 知识库 API 写入。

---

## File Structure

当前项目已有文件：

- `meeting-host-demo.html`：现有前端 Demo。后续需要把本地模拟函数替换成 `fetch` 调用。
- `项目概要.md`：项目目标、前端说明、Coze 工作流草案、接口草案。
- `实施计划.md`：本文档，作为上线执行清单。

建议上线时新增文件：

- `package.json`：定义代理服务依赖和启动脚本。
- `server.js`：本地和服务器都可运行的 Node.js API 代理。
- `.env.example`：列出需要配置的 Coze 环境变量，不保存真实 Token。
- `.gitignore`：忽略 `.env`、`node_modules` 等本地文件。
- `README.md`：记录启动方式、部署方式、验收流程。

建议 API 代理职责：

- `POST /api/transcripts/clean`：调用 Coze 工作流一，清洗转写片段。
- `POST /api/minutes/generate`：调用 Coze 工作流二，生成会议纪要。
- `POST /api/minutes/save`：调用 Coze 工作流三，写入 Coze 知识库。
- `POST /api/minutes/search`：调用 Coze 工作流四，检索历史纪要。

---

## Coze Configuration Values

上线前需要准备并记录以下值：

```text
COZE_BASE_URL=https://api.coze.cn
COZE_PAT=你的 Coze Personal Access Token
COZE_BOT_ID=会议主持人智能体 bot_id
WF_RECEIVE_TRANSCRIPT=接收会议文字工作流 workflow_id
WF_GENERATE_MINUTES=生成会议纪要工作流 workflow_id
WF_SAVE_MINUTES=保存会议纪要工作流 workflow_id
WF_SEARCH_MINUTES=历史纪要查询工作流 workflow_id
```

如果使用海外版 Coze，将 `COZE_BASE_URL` 改为对应官方 API 域名。

---

### Task 1: 创建 Coze 智能体

**Files:**
- No local file change.

**Interfaces:**
- Produces: `COZE_BOT_ID`
- Consumes: None

- [ ] **Step 1: 创建智能体**

在 Coze 控制台创建智能体：

```text
智能体名称：会议主持人
角色定位：会议主持人
```

- [ ] **Step 2: 设置角色 Prompt**

复制以下 Prompt 到智能体角色设定：

```text
你是会议主持人。你只以会议主持人身份工作，负责接收会议实时文字、整理发言内容、生成结构化会议纪要，并从历史会议纪要知识库中回答查询。

你不能扮演参会人、管理员、审核员或多个角色。所有操作都以会议主持人的口径完成。

当用户提供会议转写内容时，你需要保留原意，去除明显口语冗余，整理为清晰的会议记录。

当用户要求生成会议纪要时，你必须输出结构化 Markdown，包含：会议主题、会议摘要、关键讨论、已确认结论、待办事项、风险与遗留问题。

当用户查询历史纪要时，你必须基于知识库召回内容回答；如果知识库没有证据，需要明确说明“历史纪要中未检索到相关内容”。
```

- [ ] **Step 3: 发布智能体并记录 ID**

发布智能体后记录：

```text
COZE_BOT_ID=<你的 bot_id>
```

- [ ] **Step 4: 验收**

在 Coze 里向智能体发送：

```text
请根据一句测试转写生成会议纪要：今天确认先完成网页端 Demo，上线前接入知识库查询。
```

Expected:

```text
返回内容以会议主持人口吻输出，并包含会议主题、摘要、结论、待办、风险等结构。
```

---

### Task 2: 创建 Coze 知识库

**Files:**
- No local file change.

**Interfaces:**
- Produces: `会议纪要知识库`
- Consumes: `COZE_BOT_ID`

- [ ] **Step 1: 创建知识库**

在 Coze 中创建文档知识库：

```text
知识库名称：会议纪要知识库
知识库类型：文档知识库
```

- [ ] **Step 2: 设置切片策略**

建议配置：

```text
切片方式：自动切片或按标题切片
单片长度：800-1200 字
重叠长度：100-150 字
召回数量：3-5
```

- [ ] **Step 3: 添加一篇测试纪要**

手动上传或创建一篇测试文档：

```markdown
# 产品评审会纪要

会议 ID：mtg_seed_001
会议标题：产品评审会
创建时间：2026-07-20 10:00

## 会议主题
网页端会议主持人 Demo 的交付范围确认。

## 会议摘要
会议确认先交付网页端会议工作台，暂缓多角色协作和客户端能力。

## 关键讨论
- 第一版只开放会议主持人角色。
- 会议视频区域先使用浏览器屏幕共享。
- 历史纪要查询基于知识库完成。

## 已确认结论
优先完成网页端 Demo、纪要生成和历史查询。

## 待办事项

| 事项 | 负责人 | 截止时间 | 状态 |
| --- | --- | --- | --- |
| 准备会议样例数据 | 会议主持人 | 未明确 | 待处理 |
| 补充会议纪要模板 | 会议主持人 | 未明确 | 待处理 |
| 确认知识库结构 | 会议主持人 | 未明确 | 待处理 |

## 风险与遗留问题
需要确认 Coze 工作流是否支持直接写入知识库。
```

- [ ] **Step 4: 验收**

在 Coze 知识库检索测试里搜索：

```text
产品评审会有哪些待办事项？
```

Expected:

```text
召回内容包含“准备会议样例数据”“补充会议纪要模板”“确认知识库结构”。
```

---

### Task 3: 创建工作流一 `wf_receive_transcript`

**Files:**
- No local file change.

**Interfaces:**
- Consumes: `meeting_id: string`, `meeting_title: string`, `transcript_chunk: string`, `timestamp: string`
- Produces: `{ clean_chunk: string }`

- [ ] **Step 1: 创建工作流**

在 Coze 中创建工作流：

```text
工作流名称：wf_receive_transcript
用途：接收实时 ASR 转写片段，清洗为可用于纪要生成的会议文本。
```

- [ ] **Step 2: 配置开始节点输入**

```json
{
  "meeting_id": "string",
  "meeting_title": "string",
  "transcript_chunk": "string",
  "timestamp": "string"
}
```

- [ ] **Step 3: 添加 LLM 节点**

LLM 节点名称：

```text
清洗转写片段
```

Prompt：

```text
你是会议主持人，请清洗下面这段会议实时转写。

会议 ID：{{meeting_id}}
会议标题：{{meeting_title}}
时间点：{{timestamp}}

原始转写：
{{transcript_chunk}}

要求：
1. 保留原意。
2. 去除明显口头禅、重复词和无意义停顿。
3. 不要扩写、不要补充未出现的信息。
4. 输出 JSON，格式如下：
{
  "clean_chunk": "清洗后的文本"
}
```

- [ ] **Step 4: 配置结束节点输出**

```json
{
  "clean_chunk": "{{清洗转写片段.clean_chunk}}"
}
```

如果 Coze 节点输出不是结构化字段，结束节点改为：

```json
{
  "clean_chunk": "{{清洗转写片段.output}}"
}
```

- [ ] **Step 5: 试运行**

输入：

```json
{
  "meeting_id": "mtg_test_001",
  "meeting_title": "产品最小功能验证会议",
  "transcript_chunk": "嗯我们今天主要就是确认一下网页端 Demo，先不做多角色，先把纪要和查询跑起来。",
  "timestamp": "00:01:12"
}
```

Expected:

```json
{
  "clean_chunk": "我们今天主要确认网页端 Demo，先不做多角色，优先跑通纪要生成和历史查询。"
}
```

- [ ] **Step 6: 发布并记录 ID**

```text
WF_RECEIVE_TRANSCRIPT=<workflow_id>
```

---

### Task 4: 创建工作流二 `wf_generate_minutes`

**Files:**
- No local file change.

**Interfaces:**
- Consumes: `meeting_id: string`, `meeting_title: string`, `clean_transcript: string`
- Produces: `{ meeting_id: string, minutes_md: string }`

- [ ] **Step 1: 创建工作流**

```text
工作流名称：wf_generate_minutes
用途：根据完整会议转写生成结构化会议纪要。
```

- [ ] **Step 2: 配置开始节点输入**

```json
{
  "meeting_id": "string",
  "meeting_title": "string",
  "clean_transcript": "string"
}
```

- [ ] **Step 3: 添加 LLM 节点**

LLM 节点名称：

```text
生成结构化会议纪要
```

Prompt：

```text
你是会议主持人，请根据完整会议转写生成结构化会议纪要。

会议 ID：{{meeting_id}}
会议标题：{{meeting_title}}

完整会议转写：
{{clean_transcript}}

输出要求：
1. 输出 Markdown。
2. 必须包含：
# 会议纪要
## 会议主题
## 会议摘要
## 关键讨论
## 已确认结论
## 待办事项
## 风险与遗留问题
3. 待办事项必须使用 Markdown 表格，表头为：事项、负责人、截止时间、状态。
4. 如果负责人、截止时间、状态未明确，填写“未明确”。
5. 不要编造会议中没有的信息。
6. 语言简洁，适合直接保存到知识库。
```

- [ ] **Step 4: 配置结束节点输出**

```json
{
  "meeting_id": "{{meeting_id}}",
  "minutes_md": "{{生成结构化会议纪要.output}}"
}
```

- [ ] **Step 5: 试运行**

输入：

```json
{
  "meeting_id": "mtg_test_001",
  "meeting_title": "产品最小功能验证会议",
  "clean_transcript": "本次会议聚焦三项核心能力：会议视频转文字、自动生成结构化纪要、历史纪要知识库查询。\n产品形态限定为网页端访问，不要求用户下载安装客户端。\n当前版本只开放会议主持人一个角色。\n待办包括确认视频转写入口、完善纪要模板、配置会议纪要知识库。\n主要风险是实时转写稳定性和知识库写入权限。"
}
```

Expected:

```text
输出 Markdown，包含标题“# 会议纪要”，待办事项表格中包含“确认视频转写入口”“完善纪要模板”“配置会议纪要知识库”。
```

- [ ] **Step 6: 发布并记录 ID**

```text
WF_GENERATE_MINUTES=<workflow_id>
```

---

### Task 5: 创建工作流三 `wf_save_minutes_to_knowledge`

**Files:**
- No local file change.

**Interfaces:**
- Consumes: `meeting_id: string`, `meeting_title: string`, `minutes_md: string`, `created_at: string`
- Produces: `{ meeting_id: string, saved: boolean, knowledge_document_id: string }`

- [ ] **Step 1: 创建工作流**

```text
工作流名称：wf_save_minutes_to_knowledge
用途：将生成后的会议纪要直接写入 Coze 知识库。
```

- [ ] **Step 2: 配置开始节点输入**

```json
{
  "meeting_id": "string",
  "meeting_title": "string",
  "minutes_md": "string",
  "created_at": "string"
}
```

- [ ] **Step 3: 添加代码节点生成文档标题和正文**

代码节点名称：

```text
构造知识库文档
```

代码逻辑：

```js
const title = meeting_title || "未命名会议";
const date = created_at ? created_at.slice(0, 10) : new Date().toISOString().slice(0, 10);
const safeTitle = title.replace(/[\\/:*?"<>|]/g, "");

const doc_title = `${date}_${safeTitle}_会议纪要`;
const doc_content = `# ${title} 会议纪要

会议 ID：${meeting_id}
会议标题：${title}
创建时间：${created_at}

${minutes_md}`;

return {
  doc_title,
  doc_content
};
```

- [ ] **Step 4: 添加知识库写入节点**

配置：

```text
节点名称：写入会议纪要知识库
目标知识库：会议纪要知识库
文档标题：{{构造知识库文档.doc_title}}
文档内容：{{构造知识库文档.doc_content}}
文档类型：Markdown 或文本
```

如果 Coze 工作流画布中没有知识库写入节点，停止当前任务并记录：

```text
当前 Coze 空间不支持方案 A 的工作流直接写知识库。需要改为后端调用 Coze 知识库文档 API 写入。
```

- [ ] **Step 5: 配置结束节点输出**

```json
{
  "meeting_id": "{{meeting_id}}",
  "saved": true,
  "knowledge_document_id": "{{写入会议纪要知识库.document_id}}"
}
```

如果写入节点返回字段名不同，将结束节点里的 `document_id` 替换为 Coze 实际返回的文档 ID 字段。

- [ ] **Step 6: 试运行**

输入：

```json
{
  "meeting_id": "mtg_test_001",
  "meeting_title": "产品最小功能验证会议",
  "created_at": "2026-07-27T17:30:00+08:00",
  "minutes_md": "# 会议纪要\n\n## 会议主题\n产品最小功能验证会议\n\n## 会议摘要\n确认网页端 Demo 的后端接入路径。\n\n## 关键讨论\n- 使用 Coze 生成纪要。\n- 使用 Coze 知识库查询历史纪要。\n\n## 已确认结论\n采用方案 A，优先尝试工作流直接写入知识库。\n\n## 待办事项\n\n| 事项 | 负责人 | 截止时间 | 状态 |\n| --- | --- | --- | --- |\n| 创建 Coze 工作流 | 会议主持人 | 未明确 | 待处理 |\n\n## 风险与遗留问题\n需要确认知识库写入节点可用。"
}
```

Expected:

```text
Coze 知识库“会议纪要知识库”中新增一篇文档，标题类似“2026-07-27_产品最小功能验证会议_会议纪要”。
```

- [ ] **Step 7: 发布并记录 ID**

```text
WF_SAVE_MINUTES=<workflow_id>
```

---

### Task 6: 创建工作流四 `wf_search_minutes`

**Files:**
- No local file change.

**Interfaces:**
- Consumes: `query: string`, `meeting_scope: string`
- Produces: `{ answer: string, sources: array }`

- [ ] **Step 1: 创建工作流**

```text
工作流名称：wf_search_minutes
用途：根据用户问题检索历史会议纪要并生成答案。
```

- [ ] **Step 2: 配置开始节点输入**

```json
{
  "query": "string",
  "meeting_scope": "string"
}
```

- [ ] **Step 3: 添加知识库检索节点**

配置：

```text
节点名称：检索会议纪要知识库
目标知识库：会议纪要知识库
查询内容：{{query}}
召回数量：3-5
```

- [ ] **Step 4: 添加 LLM 节点**

LLM 节点名称：

```text
基于历史纪要回答
```

Prompt：

```text
你是会议主持人。请只根据知识库召回内容回答用户问题。

用户问题：
{{query}}

限定范围：
{{meeting_scope}}

知识库召回内容：
{{检索会议纪要知识库.output}}

回答规则：
1. 如果召回内容可以回答，给出简洁答案。
2. 必须标明来源会议标题或会议 ID。
3. 如果召回内容没有证据，回答：“历史纪要中未检索到相关内容”。
4. 不要使用知识库之外的信息。
```

- [ ] **Step 5: 配置结束节点输出**

```json
{
  "answer": "{{基于历史纪要回答.output}}",
  "sources": "{{检索会议纪要知识库.output}}"
}
```

- [ ] **Step 6: 试运行命中查询**

输入：

```json
{
  "query": "产品最小功能验证会议有哪些待办事项？",
  "meeting_scope": ""
}
```

Expected:

```text
回答包含“创建 Coze 工作流”或知识库中已有测试纪要的待办事项，并标明来源会议。
```

- [ ] **Step 7: 试运行未命中查询**

输入：

```json
{
  "query": "上季度财务审计会议的预算结论是什么？",
  "meeting_scope": ""
}
```

Expected:

```text
历史纪要中未检索到相关内容
```

- [ ] **Step 8: 发布并记录 ID**

```text
WF_SEARCH_MINUTES=<workflow_id>
```

---

### Task 7: 创建本地 API 代理

**Files:**
- Create: `package.json`
- Create: `server.js`
- Create: `.env.example`
- Create: `.gitignore`

**Interfaces:**
- Consumes: `COZE_BASE_URL`, `COZE_PAT`, `COZE_BOT_ID`, `WF_RECEIVE_TRANSCRIPT`, `WF_GENERATE_MINUTES`, `WF_SAVE_MINUTES`, `WF_SEARCH_MINUTES`
- Produces: `/api/transcripts/clean`, `/api/minutes/generate`, `/api/minutes/save`, `/api/minutes/search`

- [ ] **Step 1: 创建 `package.json`**

```json
{
  "name": "meeting-host-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
```

- [ ] **Step 2: 创建 `.env.example`**

```text
PORT=8080
COZE_BASE_URL=https://api.coze.cn
COZE_PAT=replace_with_your_coze_pat
COZE_BOT_ID=replace_with_your_bot_id
WF_RECEIVE_TRANSCRIPT=replace_with_workflow_id
WF_GENERATE_MINUTES=replace_with_workflow_id
WF_SAVE_MINUTES=replace_with_workflow_id
WF_SEARCH_MINUTES=replace_with_workflow_id
```

- [ ] **Step 3: 创建 `.gitignore`**

```text
.env
node_modules
npm-debug.log
```

- [ ] **Step 4: 创建 `server.js`**

```js
import "dotenv/config";
import express from "express";
import cors from "cors";

const app = express();
const port = Number(process.env.PORT || 8080);

app.use(cors());
app.use(express.json({ limit: "2mb" }));
app.use(express.static("."));

const requiredEnv = [
  "COZE_BASE_URL",
  "COZE_PAT",
  "COZE_BOT_ID",
  "WF_RECEIVE_TRANSCRIPT",
  "WF_GENERATE_MINUTES",
  "WF_SAVE_MINUTES",
  "WF_SEARCH_MINUTES"
];

for (const key of requiredEnv) {
  if (!process.env[key]) {
    console.warn(`[config] Missing ${key}`);
  }
}

async function runCozeWorkflow(workflowId, parameters) {
  const response = await fetch(`${process.env.COZE_BASE_URL}/v1/workflow/run`, {
    method: "POST",
    headers: {
      Authorization: `Bearer ${process.env.COZE_PAT}`,
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      workflow_id: workflowId,
      bot_id: process.env.COZE_BOT_ID,
      parameters
    })
  });

  const payload = await response.json();

  if (!response.ok || payload.code !== 0) {
    const message = payload.msg || payload.message || `Coze request failed with ${response.status}`;
    throw new Error(message);
  }

  if (typeof payload.data === "string") {
    try {
      return JSON.parse(payload.data);
    } catch {
      return { output: payload.data };
    }
  }

  return payload.data;
}

function asyncRoute(handler) {
  return async (req, res) => {
    try {
      const result = await handler(req);
      res.json(result);
    } catch (error) {
      console.error(error);
      res.status(500).json({
        error: "COZE_WORKFLOW_FAILED",
        message: error.message
      });
    }
  };
}

app.post("/api/transcripts/clean", asyncRoute(async (req) => {
  return runCozeWorkflow(process.env.WF_RECEIVE_TRANSCRIPT, {
    meeting_id: req.body.meeting_id,
    meeting_title: req.body.meeting_title,
    transcript_chunk: req.body.transcript_chunk,
    timestamp: req.body.timestamp
  });
}));

app.post("/api/minutes/generate", asyncRoute(async (req) => {
  return runCozeWorkflow(process.env.WF_GENERATE_MINUTES, {
    meeting_id: req.body.meeting_id,
    meeting_title: req.body.meeting_title,
    clean_transcript: req.body.clean_transcript
  });
}));

app.post("/api/minutes/save", asyncRoute(async (req) => {
  return runCozeWorkflow(process.env.WF_SAVE_MINUTES, {
    meeting_id: req.body.meeting_id,
    meeting_title: req.body.meeting_title,
    minutes_md: req.body.minutes_md,
    created_at: req.body.created_at
  });
}));

app.post("/api/minutes/search", asyncRoute(async (req) => {
  return runCozeWorkflow(process.env.WF_SEARCH_MINUTES, {
    query: req.body.query,
    meeting_scope: req.body.meeting_scope || ""
  });
}));

app.listen(port, () => {
  console.log(`Meeting host demo running at http://localhost:${port}/meeting-host-demo.html`);
});
```

- [ ] **Step 5: 安装依赖**

Run:

```bash
npm install
```

Expected:

```text
node_modules 创建成功，package-lock.json 创建成功。
```

- [ ] **Step 6: 创建 `.env`**

从 `.env.example` 复制一份 `.env`，填入真实值：

```text
PORT=8080
COZE_BASE_URL=https://api.coze.cn
COZE_PAT=你的真实 Token
COZE_BOT_ID=你的 bot_id
WF_RECEIVE_TRANSCRIPT=你的工作流一 ID
WF_GENERATE_MINUTES=你的工作流二 ID
WF_SAVE_MINUTES=你的工作流三 ID
WF_SEARCH_MINUTES=你的工作流四 ID
```

- [ ] **Step 7: 启动本地服务**

Run:

```bash
npm run dev
```

Expected:

```text
Meeting host demo running at http://localhost:8080/meeting-host-demo.html
```

---

### Task 8: 替换前端模拟函数为真实 API

**Files:**
- Modify: `meeting-host-demo.html`

**Interfaces:**
- Consumes: `/api/transcripts/clean`, `/api/minutes/generate`, `/api/minutes/save`, `/api/minutes/search`
- Produces: 前端真实调用 Coze 结果

- [ ] **Step 1: 增加状态变量**

在现有变量区域附近加入：

```js
let meetingId = "mtg_" + Date.now();
let cleanTranscript = [];
let currentMinutesMd = "";
```

- [ ] **Step 2: 增加 API helper**

在脚本中加入：

```js
async function postJson(url, body) {
  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify(body)
  });

  const payload = await response.json();

  if (!response.ok) {
    throw new Error(payload.message || payload.error || "请求失败");
  }

  return payload;
}
```

- [ ] **Step 3: 替换 `addNextTranscript`**

把当前模拟追加逻辑改为：

```js
async function addNextTranscript() {
  if (transcript.length >= samples.length) {
    clearInterval(transcriptTimer);
    transcriptTimer = null;
    return;
  }

  const next = samples[transcript.length % samples.length];

  try {
    const result = await postJson("/api/transcripts/clean", {
      meeting_id: meetingId,
      meeting_title: document.getElementById("meetingTitle").textContent.trim(),
      transcript_chunk: next.text,
      timestamp: next.time
    });

    const cleanText = result.clean_chunk || next.text;
    const item = {
      ...next,
      text: cleanText
    };

    transcript.push(item);
    cleanTranscript.push(`[${next.time}] ${cleanText}`);
    renderTranscript();
    minutesState.textContent = "待更新";
    saveBtn.disabled = true;
    saveBtn.textContent = "保存到知识库";
    minutesGenerated = false;
  } catch (error) {
    queryAnswer.textContent = "转写清洗失败：" + error.message;
  }
}
```

- [ ] **Step 4: 替换 `renderMinutes`**

把本地模板纪要替换为 API 调用：

```js
async function renderMinutes() {
  if (cleanTranscript.length === 0 && transcript.length === 0) {
    minutesContent.innerHTML = '<div class="empty-state">暂无实时转写内容，无法生成纪要。</div>';
    return;
  }

  minutesState.textContent = "生成中";
  minutesBtn.disabled = true;

  try {
    const result = await postJson("/api/minutes/generate", {
      meeting_id: meetingId,
      meeting_title: document.getElementById("meetingTitle").textContent.trim(),
      clean_transcript: cleanTranscript.length
        ? cleanTranscript.join("\n")
        : transcript.map((item) => `[${item.time}] ${item.text}`).join("\n")
    });

    currentMinutesMd = result.minutes_md || result.output || "";
    minutesGenerated = true;
    minutesState.textContent = "已生成";
    saveBtn.disabled = false;
    minutesContent.innerHTML = `<pre style="white-space: pre-wrap; margin: 0; font: inherit; line-height: 1.72;">${escapeHtml(currentMinutesMd)}</pre>`;
  } catch (error) {
    minutesState.textContent = "生成失败";
    minutesContent.innerHTML = `<div class="empty-state">生成纪要失败：${escapeHtml(error.message)}</div>`;
  } finally {
    minutesBtn.disabled = false;
  }
}
```

- [ ] **Step 5: 增加 HTML 转义函数**

```js
function escapeHtml(value) {
  return String(value)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;");
}
```

- [ ] **Step 6: 替换 `saveCurrentMinutes`**

```js
async function saveCurrentMinutes() {
  if (!minutesGenerated || !currentMinutesMd) {
    return;
  }

  saveBtn.disabled = true;
  saveBtn.textContent = "保存中";

  try {
    const result = await postJson("/api/minutes/save", {
      meeting_id: meetingId,
      meeting_title: document.getElementById("meetingTitle").textContent.trim(),
      minutes_md: currentMinutesMd,
      created_at: new Date().toISOString()
    });

    saveBtn.textContent = result.saved ? "已保存" : "保存完成";
    queryAnswer.textContent = "会议主持人已将当前纪要保存到会议纪要知识库，可以继续查询本次会议结论和待办。";
  } catch (error) {
    saveBtn.textContent = "保存失败";
    queryAnswer.textContent = "保存到知识库失败：" + error.message;
    saveBtn.disabled = false;
  }
}
```

- [ ] **Step 7: 替换 `queryHistory`**

```js
async function queryHistory() {
  const question = queryInput.value.trim();

  if (!question) {
    queryAnswer.textContent = "请输入需要查询的历史会议问题。";
    return;
  }

  queryBtn.disabled = true;
  queryAnswer.textContent = "正在检索历史纪要...";

  try {
    const result = await postJson("/api/minutes/search", {
      query: question,
      meeting_scope: ""
    });

    queryAnswer.textContent = result.answer || "历史纪要中未检索到相关内容";
  } catch (error) {
    queryAnswer.textContent = "历史纪要查询失败：" + error.message;
  } finally {
    queryBtn.disabled = false;
  }
}
```

- [ ] **Step 8: 本地浏览器验收**

Run:

```bash
npm run dev
```

Open:

```text
http://localhost:8080/meeting-host-demo.html
```

Expected:

```text
点击“开始会议”后，转写仍按样例流入，但文字来自 Coze 清洗结果。
点击“生成纪要”后，右上角显示 Coze 返回的 Markdown 纪要。
点击“保存到知识库”后，按钮变为“已保存”。
点击“查询历史纪要”后，右下角显示 Coze 知识库查询结果。
```

---

### Task 9: 部署上线

**Files:**
- Modify: deployment platform settings

**Interfaces:**
- Consumes: `package.json`, `server.js`, `meeting-host-demo.html`, `.env`
- Produces: public demo URL

- [ ] **Step 1: 选择部署平台**

推荐任一平台：

```text
Render：适合直接运行 Node Express。
Railway：适合快速部署 Node 服务。
Vercel：适合 Serverless，但需要把 Express 改造成 API Route。
Cloudflare Workers：适合边缘代理，但需要改写 fetch handler。
```

当前 `server.js` 最适合先部署到 Render 或 Railway。

- [ ] **Step 2: 设置环境变量**

在部署平台后台配置：

```text
PORT=8080
COZE_BASE_URL=https://api.coze.cn
COZE_PAT=你的真实 Token
COZE_BOT_ID=你的 bot_id
WF_RECEIVE_TRANSCRIPT=你的工作流一 ID
WF_GENERATE_MINUTES=你的工作流二 ID
WF_SAVE_MINUTES=你的工作流三 ID
WF_SEARCH_MINUTES=你的工作流四 ID
```

- [ ] **Step 3: 设置启动命令**

```bash
npm install
npm start
```

- [ ] **Step 4: 访问线上 Demo**

Open:

```text
https://你的部署域名/meeting-host-demo.html
```

Expected:

```text
页面能打开，浏览器控制台没有 404 或 500。
```

---

### Task 10: 端到端上线验收

**Files:**
- No local file change.

**Interfaces:**
- Consumes: deployed demo URL
- Produces: 上线验收结果

- [ ] **Step 1: 开始会议**

操作：

```text
点击“开始会议”，授权屏幕共享。
```

Expected:

```text
左上角出现屏幕共享画面，左下角开始出现转写内容。
```

- [ ] **Step 2: 生成纪要**

操作：

```text
等待至少 3 条转写出现后，点击“生成纪要”。
```

Expected:

```text
右上角出现 Markdown 会议纪要，包含会议主题、摘要、关键讨论、结论、待办、风险。
```

- [ ] **Step 3: 保存到知识库**

操作：

```text
点击“保存到知识库”。
```

Expected:

```text
按钮显示“已保存”，Coze 知识库中新增一篇会议纪要文档。
```

- [ ] **Step 4: 查询命中问题**

输入：

```text
这次会议有哪些待办事项？
```

Expected:

```text
右下角回答包含待办事项，并标明来源会议。
```

- [ ] **Step 5: 查询未命中问题**

输入：

```text
上季度财务审计会议的预算结论是什么？
```

Expected:

```text
历史纪要中未检索到相关内容
```

- [ ] **Step 6: 安全检查**

检查浏览器源代码、Network 请求和控制台：

```text
页面中不能出现 COZE_PAT。
Network 请求只能看到你的代理 API，不能直接暴露 Authorization Bearer Token。
```

Expected:

```text
Coze Token 只存在部署平台环境变量中，不出现在前端代码和浏览器请求里。
```

---

## Rollout Checklist

- [ ] Coze 智能体 `会议主持人` 已发布。
- [ ] Coze 知识库 `会议纪要知识库` 已创建。
- [ ] `wf_receive_transcript` 已发布并记录 workflow_id。
- [ ] `wf_generate_minutes` 已发布并记录 workflow_id。
- [ ] `wf_save_minutes_to_knowledge` 已发布并记录 workflow_id。
- [ ] `wf_search_minutes` 已发布并记录 workflow_id。
- [ ] API 代理本地运行成功。
- [ ] 前端已从本地模拟函数切换为 API 调用。
- [ ] 线上部署环境变量已配置。
- [ ] 线上 Demo 可以完成：开始会议、生成纪要、保存知识库、查询历史纪要。
- [ ] 浏览器中没有暴露 Coze Token。

## Known Risks

- Coze 工作流画布可能没有知识库写入节点；如果没有，方案 A 的“直接写知识库”无法完成。
- Coze API 返回的 `data` 可能是字符串，也可能是对象；代理层需要兼容两种情况。
- 知识库写入后可能有索引延迟；保存后立即查询可能短暂查不到，演示时可以等待 10-30 秒。
- 第一版仍使用模拟转写，真实 ASR 需要后续接入屏幕共享音频或麦克风音频。

## Next Phase

真实 ASR 接入建议单独开一个阶段：

```text
浏览器采集音频
  -> WebSocket 上传音频片段
  -> ASR 服务转写
  -> /api/transcripts/clean
  -> 前端实时展示
```

该阶段需要新增音频权限、音频编码、流式传输、ASR 服务选型和断线重连机制，不建议混在当前上线 Demo 中一起做。
