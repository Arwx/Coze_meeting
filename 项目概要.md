# 会议主持人项目概要

## 1. 项目目标

本项目用于搭建一个网页端会议主持人工作台，并在 Coze 平台上实现会议相关自动化能力。

当前产品只开放一个角色：会议主持人。会议主持人负责完成会议开始、会议视频查看、实时转写、结构化会议纪要生成、会议纪要保存和历史纪要查询。

核心业务链路：

1. 会议视频/屏幕实时接入
2. 会议语音实时转文字
3. 自动生成结构化会议纪要
4. 保存会议纪要到知识库
5. 基于历史会议纪要进行查询

## 2. 当前前端 Demo

文件位置：

```text
D:\my_app\Meeting\meeting-host-demo.html
```

当前页面是单文件 HTML，可直接用浏览器打开。如果浏览器限制屏幕共享能力，建议在项目目录启动本地服务后访问：

```bash
python -m http.server 8080
```

然后打开：

```text
http://localhost:8080/meeting-host-demo.html
```

页面布局为 2x2 工作台：

| 区域 | 功能 |
| --- | --- |
| 左上：会议视频 | 点击“开始会议”后调用浏览器屏幕共享能力，显示本地屏幕/窗口/标签页 |
| 右上：结构化会议纪要 | 根据实时转写内容生成摘要、讨论、结论、待办和风险 |
| 左下：实时转写 | 当前用本地模拟数据自动流入，后续替换为真实 ASR 输出 |
| 右下：历史纪要查询 | 当前用本地模拟知识库数据回答，后续接 Coze 知识库检索 |

当前支持的按钮：

| 按钮 | 当前行为 | 后续真实能力 |
| --- | --- | --- |
| 开始会议 | 请求浏览器屏幕共享，并启动模拟转写 | 创建会议会话，接入视频/音频流 |
| 停止会议 | 停止屏幕共享和模拟转写，会议状态变为已结束 | 结束会议会话，关闭实时任务 |
| 生成纪要 | 根据当前转写生成本地模拟纪要 | 调用 Coze 纪要生成工作流 |
| 保存到知识库 | 将当前纪要保存到本地模拟历史列表 | 写入 Coze 知识库或后端数据库 |
| 查询历史纪要 | 基于本地模拟历史数据回答 | 调用 Coze 知识库检索工作流 |

## 3. Coze 角色设定

智能体名称建议：

```text
会议主持人
```

角色 Prompt 建议：

```text
你是会议主持人。你只以会议主持人身份工作，负责接收会议实时文字、整理发言内容、生成结构化会议纪要，并从历史会议纪要知识库中回答查询。

你不能扮演参会人、管理员、审核员或多个角色。所有操作都以会议主持人的口径完成。

当用户提供会议转写内容时，你需要保留原意，去除明显口语冗余，整理为清晰的会议记录。

当用户要求生成会议纪要时，你必须输出结构化 Markdown，包含：会议主题、会议摘要、关键讨论、已确认结论、待办事项、风险与遗留问题。

当用户查询历史纪要时，你必须基于知识库召回内容回答；如果知识库没有证据，需要明确说明“历史纪要中未检索到相关内容”。
```

## 4. Coze 工作流设计

### 4.1 工作流一：接收会议文字

用途：接收实时 ASR 转写片段，并整理为可用于纪要生成的会议文本。

输入参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| meeting_id | string | 会议唯一 ID |
| meeting_title | string | 会议标题 |
| transcript_chunk | string | 实时转写片段 |
| timestamp | string | 片段时间点 |

处理步骤：

1. 开始节点接收参数
2. LLM 节点清洗转写片段
3. 将清洗后的内容追加到当前会议记录
4. 返回本次整理后的片段和当前累计条数

输出参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| clean_chunk | string | 清洗后的转写片段 |
| transcript_count | number | 当前会议已有转写条数 |

### 4.2 工作流二：生成会议纪要

用途：根据当前会议转写生成结构化会议纪要。

输入参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| meeting_id | string | 会议唯一 ID |
| meeting_title | string | 会议标题 |
| clean_transcript | string | 完整会议转写 |

输出格式：

```markdown
# 会议纪要

## 会议主题

## 会议摘要

## 关键讨论

## 已确认结论

## 待办事项

| 事项 | 负责人 | 截止时间 | 状态 |
| --- | --- | --- | --- |

## 风险与遗留问题
```

### 4.3 工作流三：保存会议纪要

用途：将生成后的会议纪要写入知识库，供后续查询复用。

输入参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| meeting_id | string | 会议唯一 ID |
| meeting_title | string | 会议标题 |
| minutes_md | string | Markdown 会议纪要 |
| created_at | string | 创建时间 |

保存目标：

```text
会议纪要知识库
```

文档标题建议：

```text
YYYY-MM-DD_会议主题_会议纪要
```

如果当前 Coze 空间暂不支持工作流直接写入知识库，可以先使用后端数据库保存纪要，再定期同步到 Coze 知识库。

### 4.4 工作流四：历史纪要查询

用途：根据用户问题检索历史会议纪要并生成回答。

输入参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| query | string | 用户查询问题 |
| meeting_scope | string | 可选，限定某个会议或时间范围 |

处理步骤：

1. 知识库检索节点召回相关纪要片段
2. LLM 节点基于召回内容回答
3. 返回答案和来源会议

回答规则：

1. 必须基于知识库内容回答
2. 命中内容时标明来源会议
3. 未命中时返回：历史纪要中未检索到相关内容

## 5. 后端建议接口

后端可以作为前端和 Coze 之间的中间层，负责会议状态、转写片段、纪要结果和知识库同步。

### 5.1 会议会话

```http
POST /api/meetings/start
```

请求：

```json
{
  "meeting_title": "产品最小功能验证会议"
}
```

响应：

```json
{
  "meeting_id": "mtg_20260727_001",
  "status": "recording"
}
```

```http
POST /api/meetings/{meeting_id}/stop
```

响应：

```json
{
  "meeting_id": "mtg_20260727_001",
  "status": "ended"
}
```

### 5.2 实时转写

```http
POST /api/meetings/{meeting_id}/transcripts
```

请求：

```json
{
  "timestamp": "00:03:28",
  "text": "产品形态先限定为网页端访问...",
  "source": "asr"
}
```

响应：

```json
{
  "clean_chunk": "产品形态限定为网页端访问...",
  "transcript_count": 12
}
```

### 5.3 生成纪要

```http
POST /api/meetings/{meeting_id}/minutes/generate
```

响应：

```json
{
  "meeting_id": "mtg_20260727_001",
  "minutes_md": "# 会议纪要\n\n## 会议主题\n..."
}
```

### 5.4 保存纪要

```http
POST /api/meetings/{meeting_id}/minutes/save
```

响应：

```json
{
  "meeting_id": "mtg_20260727_001",
  "knowledge_document_id": "doc_001",
  "saved": true
}
```

### 5.5 历史查询

```http
POST /api/minutes/search
```

请求：

```json
{
  "query": "这次会议有哪些待办事项？"
}
```

响应：

```json
{
  "answer": "相关待办包括：确认视频转写入口、完善纪要模板、配置会议纪要知识库。",
  "sources": [
    {
      "meeting_id": "mtg_20260727_001",
      "meeting_title": "产品最小功能验证会议"
    }
  ]
}
```

## 6. 建议数据结构

### meetings

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 会议 ID |
| title | string | 会议标题 |
| status | string | pending / recording / ended |
| started_at | datetime | 开始时间 |
| ended_at | datetime | 结束时间 |
| created_by | string | 主持人 ID |

### transcript_chunks

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 片段 ID |
| meeting_id | string | 会议 ID |
| timestamp | string | 视频时间点 |
| raw_text | text | 原始转写 |
| clean_text | text | 清洗后文本 |
| created_at | datetime | 创建时间 |

### meeting_minutes

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 纪要 ID |
| meeting_id | string | 会议 ID |
| title | string | 纪要标题 |
| minutes_md | text | Markdown 纪要 |
| knowledge_document_id | string | Coze 知识库文档 ID |
| created_at | datetime | 创建时间 |

## 7. 当前未完成事项

1. 真实 ASR：当前前端只显示本地屏幕，实时转写仍是模拟数据。
2. Coze 工作流：需要在 Coze 中创建接收文字、生成纪要、保存知识库、历史查询工作流。
3. 知识库写入：需要确认当前 Coze 空间是否支持工作流直接写入知识库。
4. 后端服务：需要实现会议会话、转写片段、纪要生成、纪要保存、历史查询接口。
5. 音频来源：如果要从屏幕共享中获取音频，需要浏览器授权共享音频，并接入可用 ASR 服务。

## 8. 下一步建议

1. 先在 Coze 创建智能体：会议主持人。
2. 创建知识库：会议纪要知识库。
3. 先搭工作流二和工作流四：生成纪要、历史纪要查询。
4. 后端先实现会议和纪要保存接口。
5. 最后接入 ASR，把模拟转写替换成真实转写流。
